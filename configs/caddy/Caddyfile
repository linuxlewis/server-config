# Caddy reverse proxy configuration
# Used for public-facing apps exposed via Cloudflare Tunnel
# Private services (code-server) are served via Tailscale Serve

# Example: public app behind Cloudflare Tunnel
# app.example.com {
# 	reverse_proxy app-container:8080
# }
