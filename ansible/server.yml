---
- name: Configure dev server
  hosts: servers
  become: true

  vars:
    username: dev
    docker_data_dir: /opt/services
    ci_mode: false
    tailscale_authkey: "{{ lookup('env', 'TAILSCALE_AUTHKEY') }}"

  roles:
    - base
    - docker
    - dev
    - networking
